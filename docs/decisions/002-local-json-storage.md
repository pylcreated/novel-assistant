# 决策 002：本地 JSON 存储

## 决策背景
项目目标为本地单机可用，需低门槛、低复杂度的持久化方案。

## 采用方案
采用本地 JSON 文件存储（每项目一个文件），并在保存/删除前做文件备份。

## 不采用方案
- 引入数据库（超出当前复杂度目标）
- 云端托管存储（增加部署与权限成本）

## 当前影响
- 数据可直接查看和迁移
- 与 FastAPI 接口耦合较低，便于快速迭代

## 后续风险
- 并发写入冲突风险
- 文件损坏后的恢复依赖备份质量
