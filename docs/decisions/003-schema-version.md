# 决策 003：schema_version 与迁移机制

## 决策背景
版本迭代后字段会变化，旧项目若无迁移机制会导致读取失败或前端报错。

## 采用方案
引入 `schema_version` 并在读取时执行版本迁移；保存时规范化为当前版本（v0.4）。

## 不采用方案
- 手工逐项目升级（不可持续）
- 遇到旧字段直接报错（破坏可用性）

## 当前影响
- 旧项目可在新版本中继续使用
- 前后端可在兼容层并行支持历史字段

## 后续风险
- 双字段并存（如 `plot_threads/plotlines`）可能漂移
- 迁移链变长后测试成本增加
